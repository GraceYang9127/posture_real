import React from 'react';
import VideoButton from '../components/videoButton.jsx';

const Camera = () => {
  return <div>
    <h1>Camera</h1>
    <VideoButton />
    </div>;
};

export default Camera;
